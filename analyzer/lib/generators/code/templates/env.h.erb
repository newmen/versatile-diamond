#ifndef ENV_H
#define ENV_H

#include <tools/yaml_config_reader.h>
using namespace vd;

class Env
{
    static YAMLConfigReader __config;

public:
    static double R();

    static double gasT();
    static double surfaceT();

<% gas_species.each do |spec| %>
    static double <%= concentration_method(spec) %>;
<% end %>
};

#endif // ENV_H
