#include "<%= file_name %>.h"
<% source_species_dependencies.each do |specie| %>
#include "../<%= specie.full_file_path %>.h"
<% end %>

<% unless symmetric? && render_find_algorithms? %>
const ushort <%= class_name %>::__indexes[<%= atoms_num %>] = { <%= indexes_sequence %> };
const ushort <%= class_name %>::__roles[<%= atoms_num %>] = { <%= roles_sequence %> };

<%= render_partial('name_method.cpp') %>
<% end %>

<% if render_find_algorithms? %>
<%= render_partial('find_algorithms.cpp') %>
<% end %>
