#if defined(PRINT) || defined(SPEC_PRINT) || defined(JSONLOG)
const char *<%= class_name %>::name() const
{
    static const char value[] = "<%= print_name %>";
    return value;
}
#endif // PRINT || SPEC_PRINT || JSONLOG
