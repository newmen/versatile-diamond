public:
    static void find(<%= find_arguments_str %>);
<% if sidepiece? %>
<% wheres.each do |where| %>
    template <class L> static void <%= where.signature %>;
<% end %>
<% end %>

protected:
<% if parent? %>
    void findAllChildren() final;
<% end %>
<% if specific? %>
    void findAllTypicalReactions() final;
<% end %>
<% if sidepiece? %>
    void findAllLateralReactions() final;
<% end %>
<% if local? %>
    void concretizeLocal(Atom *target) const final;
    void unconcretizeLocal(Atom *target) const final;
<% end %>
