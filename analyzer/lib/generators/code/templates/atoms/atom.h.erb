#ifndef <%= define_name %>
#define <%= define_name %>

#include "specified_atom.h"

class <%= class_name %> : public SpecifiedAtom<<%= valence %>>
{
    static const char __name[];

public:
    <%= class_name %>(ushort type, ushort actives, Lattice *lattice) : SpecifiedAtom(type, actives, lattice) {}

    const char *name() const { return __name; }
};

#endif // <%= define_name %>
