#include "<%= file_name %>.h"
<%= body_includes %>

<%= render_rate_and_name_body_partial %>

void <%= class_name %>::createUnconcreted(LateralSpec *removableSpec)
{
    <%= assert_removable_spec_str %>;
    <%= create_parent_reaction_str %>;
}
